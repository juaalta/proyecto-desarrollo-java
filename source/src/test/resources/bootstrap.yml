server:
  port: 0

spring:
  application:
    name: service
  profiles:
    active: test
  cloud:
    discovery:
      enabled: false
    config:
      enabled: false
      discovery:
        enabled: false
    loadbalancer:
      ribbon:
        enabled: false
  sleuth:
    enabled: false

eureka:
  client:
    enabled: false

springdoc:
  api-docs:
    path: "/openapi/v3"
